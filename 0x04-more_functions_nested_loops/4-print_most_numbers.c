#include "main.h"

/**
 * print_numbers: print 0 _ 9
 *
 * Return: Always 0 (Success)
*/

void print(void)
{
	int num;
	for (num == 0; num <= 9; num++)
	[
		if (num == 2 || num == 4)
			continue
			_putchar(num + 48);
	}
	_putchar('\n');
{
